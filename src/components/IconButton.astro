---
const { href, class: className } = Astro.props;
---

<a
  class={`rounded-lg flex justify-center items-center py-2 px-2 hover:scale-110 hover:bg-white/5 transition ${className}`}
  rel="noopener noreferrer"
  href={href}
>
  <slot />
</a>
